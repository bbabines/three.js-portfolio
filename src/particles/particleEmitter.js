import * as THREE from 'three'

// Eventually combine all particle logic here for more complex scenarios
export class ParticleEmitter {
    constructor(scene) {
        this.scene = scene
        
        // this.particleSystem 
        // this.scene.add(this.particleSystem)
    }
}
